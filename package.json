{
  "name": "offlinecrm-demo-electron-autoupdate",
  "version": "0.0.1",
  "description": "",
  "main": "packages/app/index.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-builder build --win --publish never",
    "deploy": "electron-builder build --win --publish always",
    "up:version": "lerna version --no-push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "offlinecrm",
    "electron"
  ],
  "author": "Nikolay Gerzhan <nikolay.grzhan@helpcontrol.ru>",
  "license": "MIT",
  "devDependencies": { 
    "jest": "^25.1.0",
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "electron-updater": "4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/offlinecrm/offlinecrm-demo-electron-autoupdate.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "private": true
}
